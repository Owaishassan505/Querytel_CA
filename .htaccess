RewriteEngine On

# Only rewrite if the request isn't for a real file or folder
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite URLs to .php equivalents
RewriteRule ^(.+?)/?$ $1.php [L]
